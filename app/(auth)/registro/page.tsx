import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
    title: "Crear cuenta - Findmi",
    description: "Unite a la comunidad de músicos de Findmi",
};

export default function RegisterPage() {
    return <RegisterForm />;
}
